name: Build ISO
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'build-disk.yml'
      - 'disk_config/iso.toml'

jobs:
  build-iso:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build ISO with bootc-image-builder
        uses: osbuild/bootc-image-builder-action@v0.0.2
        with:
          # --- CRITICAL FIX: Add "./" prefix ---
          # This tells Podman "This is a file path", not a volume name.
          config-file: ./disk_config/iso.toml
          
          image: ghcr.io/daedaluscomedown-beep/deconfliction:latest
          types: iso
          additional-args: --use-librepo=True

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v6
        with:
          name: bazzite-deconfliction-install.iso
          path: output/bootiso/*.iso
          if-no-files-found: error