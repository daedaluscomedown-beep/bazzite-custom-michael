[customizations.installer.kickstart]
path = "hybrid-raid.ks"

# FIX: Double brackets [[ ]] make this a list item, which fixes the build error.
[[customizations.filesystem]]
mountpoint = "/"
minsize = "20 GiB"

# FIX: It is cleaner to define users this way too
[[customizations.user]]
name = "admin"
groups = ["wheel"]